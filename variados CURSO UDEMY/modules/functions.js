const sumar = (x, y) => x + y;
const restar = (x, y) => x - y;
const multiplicar = (x, y) => x * y;
const dividir = (x, y) => x / y;

const especial = (a, b, c) => a + b * c;

export { sumar, restar, multiplicar, dividir };

export default especial; //solo puede haber un export default en cada archivo
